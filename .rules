你是这个项目的 AI 编码搭档。请严格遵守以下规则，除非用户明确要求偏离，否则一律按本规则执行。

# 1. 项目定位
- 这是一个基于 **Vue 3 + TypeScript + Vite + Pinia + Vue Router + Arco Design Vue** 的前端，和 **Python 3.11 + FastAPI + SQLAlchemy + SQLite + ChromaDB** 的后端项目。
- 这是一个 **AI 知识库 / RAG Demo 项目**，目标是：可本地运行、可演示、可答辩、可扩展。
- 实现时优先兼顾：
  1. 可读性
  2. 可维护性
  3. Demo 可落地性
  4. 面试可讲解性
- 不要为了“看起来高级”而引入超出项目体量的复杂方案。

# 2. 全局工程原则
- **MVP 优先**：先保证主链路可用，再做增强功能。
- **前端体验优先**：所有关键异步操作必须有 loading、success、empty、error 状态。
- **AI 能力必须可解释**：所有 RAG、检索、引用、流式返回相关实现，都必须保证链路清晰、便于讲解。
- **避免过度设计**：不要无故引入微服务、复杂权限系统、事件总线滥用、过深抽象层。
- **单一职责**：页面、组件、store、service、router、schema、model 要分层明确。
- **稳定优先于炫技**：优先使用成熟、直接、易调试的实现。

# 3. AI 行为约束
- 在开始编码前，先理解当前任务属于：前端页面、前端逻辑、后端接口、数据模型、AI/RAG链路、工程配置中的哪一类。
- 修改前必须先阅读与任务强相关的现有文件，再动手改。
- 优先复用项目已有模式，不要凭空创造一套新风格。
- 若用户没有明确要求，不要主动重构大段无关代码。
- 若修改范围可能影响多个模块，先说明影响面，再实施。
- 新增代码时，默认补齐必要的类型定义、错误处理、边界状态和注释（只写必要注释，避免废话注释）。
- 输出代码时，优先给出：
  1. 方案结论
  2. 变更点
  3. 关键代码
  4. 如何验证
- 不要输出“伪完整方案”却不落地；如果给代码，必须尽量可直接放入项目中使用。

# 4. 文件与目录约定

## 4.1 前端目录建议
- `frontend/src/api/`：接口请求封装，按业务模块拆分
- `frontend/src/views/`：页面级组件
- `frontend/src/components/`：可复用组件与业务组件
- `frontend/src/stores/`：Pinia store
- `frontend/src/router/`：路由定义
- `frontend/src/types/`：全局类型与领域模型类型
- `frontend/src/utils/`：纯工具函数
- `frontend/src/constants/`：常量、枚举映射、配置项
- `frontend/src/composables/`：可复用组合式逻辑

## 4.2 后端目录建议
- `backend/app/api/`：路由层，只做参数处理、鉴权占位、返回封装
- `backend/app/services/`：业务逻辑层
- `backend/app/models/`：SQLAlchemy 模型
- `backend/app/schemas/`：Pydantic 请求/响应模型
- `backend/app/core/`：配置、数据库连接、基础依赖、LLM 客户端
- `backend/app/utils/`：通用工具函数

## 4.3 分层约束
- 前端页面不要直接写复杂请求细节，统一走 `api/`
- 前端页面不要堆积过多业务逻辑，复杂逻辑下沉到 composables/store/utils
- 后端路由不要写业务细节，业务逻辑必须下沉到 `services/`
- 数据库模型、请求 schema、响应 schema 不要混写

# 5. 前端代码规范

## 5.1 技术与写法
- 使用 Vue 3 `script setup` + TypeScript。
- 优先使用 Composition API，不使用 Options API。
- 所有 props、emits、响应数据、接口返回值，尽量显式声明类型。
- 禁止滥用 `any`；确实无法避免时，优先用 `unknown` 再做收窄。
- 页面组件负责组织结构，业务逻辑尽量抽离。

## 5.2 组件设计
- 一个组件只负责一个清晰职责。
- 单文件组件建议控制在 **100~300 行**，超过则拆分。
- 复杂页面必须拆成：页面容器 + 业务组件 + 通用组件。
- 可复用逻辑优先抽成 composables，而不是复制粘贴。
- 组件命名使用 PascalCase，文件名与组件名保持一致。

## 5.3 状态管理
- 跨页面或跨组件共享状态使用 Pinia。
- 页面内部临时状态优先使用组件本地状态，不要什么都塞进 store。
- store 中只放：共享状态、派生状态、必要动作。
- 禁止 store 变成“万能垃圾桶”。

## 5.4 API 调用
- 所有请求统一封装在 `api/` 目录。
- 接口命名清晰，按业务域拆分，如 `kb.ts`、`knowledge.ts`、`chat.ts`、`agent.ts`。
- API 返回类型必须定义清楚。
- 页面层不要直接拼接复杂请求参数和响应转换逻辑。

## 5.5 UI 与交互
- 风格默认遵循：**企业级后台 + AI 助手 + 知识管理平台**。
- 页面应强调：清晰、克制、信息层级明确。
- 所有异步行为必须覆盖以下状态：
  - idle
  - loading
  - success
  - empty
  - error
- 删除、重置、危险操作必须二次确认。
- 表单提交必须有禁用态、提交态和错误反馈。
- 聊天、上传、解析进度、检索、流式问答等强异步场景必须有清晰反馈。

## 5.6 样式规范
- 优先保持项目现有样式方案一致。
- 若无既有规范，优先使用 scoped 样式或清晰的模块化样式组织。
- 样式命名清晰、稳定，不写无语义缩写。
- 避免魔法数；颜色、圆角、间距、阴影尽量抽成变量或统一设计 token。
- 不要堆砌炫酷效果，优先保证一致性和可读性。

## 5.7 文案规范
- 默认直接使用中文文案，除非用户明确要求国际化。
- 提示文案要简洁、明确、贴近真实产品。
- 错误文案不要只写“失败了”，应说明原因或下一步建议。

# 6. 后端代码规范

## 6.1 总体原则
- 使用 FastAPI + Pydantic + SQLAlchemy 2.x。
- 路由层轻，服务层重。
- 模型、schema、service、router 分层清晰。
- 所有核心逻辑要便于后续接入 RAG、向量检索、异步任务。

## 6.2 路由规范
- 路由按业务域拆分，如：
  - `/api/kb`
  - `/api/knowledge`
  - `/api/upload`
  - `/api/chat`
  - `/api/agent`
  - `/api/stats`
- 路由函数只负责：
  - 参数接收
  - 基础校验
  - 调用 service
  - 返回 response
- 不在路由中直接写复杂数据库操作、检索逻辑、prompt 拼装逻辑。

## 6.3 Schema 规范
- 请求体与响应体使用 Pydantic schema 明确描述。
- 不要直接返回 ORM 对象给前端。
- 列表、详情、创建、更新的 schema 分开定义，避免混乱。

## 6.4 Service 规范
- 业务逻辑统一放到 `services/`。
- 每个 service 聚焦一个领域，如：
  - `kb_service`
  - `knowledge_service`
  - `parser_service`
  - `chunk_service`
  - `embedding_service`
  - `retriever_service`
  - `rag_service`
- service 函数命名清晰，体现动作和对象。

## 6.5 数据模型规范
- 关系型数据库负责元数据与状态管理。
- 向量数据库负责 embedding 与语义检索索引。
- 模型字段命名清晰统一，避免缩写。
- 通用字段建议统一：`id`, `created_at`, `updated_at`。
- 状态字段建议使用枚举或受控常量，不要散落魔法字符串。

## 6.6 错误处理
- 统一定义错误响应风格。
- 对用户可见错误要给出明确原因。
- 对内部异常记录必要日志，但不要向前端暴露堆栈。
- 不要用裸 `except Exception` 吞掉错误；如必须兜底，要保留日志。

# 7. RAG / AI 链路规范
- 所有 AI 功能必须围绕以下链路组织：
  1. 文档导入
  2. 文档解析
  3. chunk 切分
  4. embedding 向量化
  5. 检索
  6. prompt 组装
  7. LLM 回答
  8. references 返回
- 禁止把所有逻辑写进一个 `chat` 接口里。
- Prompt 模板必须单独组织，便于维护和解释。
- 检索结果必须能映射到引用源展示，不可只给模型不留前端可解释信息。
- 当上下文不足时，优先返回“信息不足”，而不是编造答案。
- 流式问答中，正文与 references 建议分开返回。

# 8. 工程规范

## 8.1 命名规范
- 文件名、目录名、函数名要语义化。
- 前端组件使用 PascalCase。
- TypeScript 变量和函数使用 camelCase。
- Python 文件、函数、变量使用 snake_case。
- 常量使用 UPPER_SNAKE_CASE。

## 8.2 Git 规范
- 推荐分支：`main`、`dev`、`feat/*`、`fix/*`、`docs/*`。
- Commit 推荐使用 Conventional Commits：
  - `feat:`
  - `fix:`
  - `docs:`
  - `refactor:`
  - `chore:`
  - `test:`

## 8.3 代码修改范围
- 只改与当前任务直接相关的内容。
- 不顺手大改无关文件。
- 若发现结构问题，可提出建议，但默认不主动大重构。

## 8.4 文档规范
- 重要模块或关键取舍变更后，提醒同步更新 README 或架构文档。
- 新增复杂模块时，补充必要说明，确保后续可维护和可答辩。

# 9. 质量与验证规范
- 生成代码后，默认自检：
  1. 是否符合当前项目技术栈
  2. 是否命名清晰
  3. 是否类型完整
  4. 是否有边界状态处理
  5. 是否有明显的重复代码
  6. 是否容易被面试官追问时解释清楚
- 前端变更要重点检查：
  - 是否缺 loading / empty / error
  - 是否把过多逻辑堆在页面里
  - 是否有类型遗漏
- 后端变更要重点检查：
  - 是否违反分层
  - 是否 schema/model/service 混杂
  - 是否缺少错误处理
- 如用户未要求，不默认引入测试框架或大规模补测试；但新增复杂逻辑时，应给出建议的测试点。

# 10. 默认输出风格
- 先给结论，再给实现。
- 说明为什么这样做，而不只是贴代码。
- 如果存在取舍，明确说明推荐方案和原因。
- 尽量补一句“面试时可以怎么讲”。

# 11. 禁止事项
- 禁止无故更换技术栈。
- 禁止引入与项目体量不匹配的复杂架构。
- 禁止为了一次需求创建大量抽象层。
- 禁止大面积使用 `any`、裸字典、无类型返回。
- 禁止在前端页面中塞入大段不可维护逻辑。
- 禁止在后端路由中写核心业务逻辑。
- 禁止忽略错误状态和空状态。
- 禁止输出“仅供参考、不能直接运行”的大段伪代码来冒充实现。

# 12. 默认工作方式
当接到需求时，按以下顺序执行：
1. 先判断任务归属模块
2. 阅读相关已有代码
3. 复用现有模式
4. 给出实现方案
5. 再补充增强建议
6. 最后说明验证方法与后续可扩展点
